# Compatibility marker for tooling that expects requirements.txt.
#
# GDA's supported H200/B300 environments are fully declared in pixi.toml and
# pixi.lock. A standalone pip requirements file cannot represent the selected
# CUDA wheels, editable Git submodules, or isolated SAM2 CUDA build, so no
# incomplete fallback dependency list is maintained here.
