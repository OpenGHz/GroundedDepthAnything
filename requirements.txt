# Legacy requirements file. Use pixi.toml/pixi.lock for the supported runtime.
# This list is retained only for older sdf_compute scripts.
# common
torch>=2
torchvision
# da3
xformers
# others
supervision>=0.18.0 # 用于标注和可视化

# post-processing
open3d>=0.19.0
